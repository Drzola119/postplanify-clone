/**
 * Storage keys for client-side persistence. Centralized here so we don't
 * sprinkle string literals across components.
 */

export const ACTIVE_WORKSPACE_STORAGE_KEY = "postplanify.activeWorkspaceId";
